import { Cli } from '@libero/migrator';
import { Config as KnexConfig } from 'knex';

/*
const knexConfig: KnexConfig = {
    client: 'sqlite3',
    connection: {
        filename: './data.db',
    },
};

const cli = new Cli({
    banner: 'Libero Reviewer Submission: Migration tool',
    name: 'migrate',
    knexConfig: knexConfig,
    migrations: {
        path: `${__dirname}/migrations`,
        pattern: /.*\.sql$/,
    },
});

cli.exec();
*/
