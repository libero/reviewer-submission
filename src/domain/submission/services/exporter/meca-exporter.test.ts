import MecaExporter from './meca-exporter';

describe('MecaExporter', () => {
    it('should export archive', () => {
        const fileService = {
            
        };
        const exporter = new MecaExporter(fileService);
    })
});