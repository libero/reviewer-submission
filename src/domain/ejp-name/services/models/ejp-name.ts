export default class EJPName {
    id: number;
    first: string;
    last: string;

    constructor(id: number, first: string, last: string) {
        this.id = id;
        this.first = first;
        this.last = last;
    }
}
